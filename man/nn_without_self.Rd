% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn.R
\name{nn_without_self}
\alias{nn_without_self}
\title{Nearest neighbours excluding the self match}
\usage{
nn_without_self(
  data,
  k,
  backend = c("auto", "cpu", "cuda"),
  method = c("auto", "exact", "flat", "bruteforce", "grid", "vptree", "sparse", "hnsw",
    "ivf", "ivfpq", "nsg", "nndescent", "cagra"),
  metric = c("euclidean", "cosine", "correlation", "inner_product"),
  tuning = c("auto", "cache", "pilot", "fixed", "off", "none"),
  output = c("double", "float"),
  n_threads = NULL
)
}
\arguments{
\item{data}{Numeric matrix/data frame or sparse `Matrix` object with
observations in rows.}

\item{k}{Number of non-self neighbours to return.}

\item{backend}{Device backend: `"auto"`, `"cpu"`, or `"cuda"`. `"auto"`
follows \code{\link{nn}()} backend/method/metric resolution, using CUDA
only for validated CUDA combinations when CUDA/cuVS runtime support is
available, and CPU otherwise.}

\item{method}{Algorithm selector passed through the same resolver as
\code{\link{nn}()}. See \code{\link{nn}()} for method descriptions and
references.}

\item{metric}{Distance metric: `"euclidean"`, `"cosine"`, `"correlation"`,
or `"inner_product"`; aliases such as `"l2"`, `"cor"`/`"pearson"`, and
`"ip"` are accepted. Correlation is centered cosine similarity, not raw
inner product; see \code{\link{nn}()} for metric/backend support details,
including metric-aware CPU HNSW routing.}

\item{tuning}{Tuning policy passed to \code{\link{nn}()}. `"auto"` uses the
tuned default for the resolved method.}

\item{output}{Distance storage type: `"double"` for the default R numeric
matrix or `"float"` for a `float::fl()`/`float32` distance matrix when the
optional `float` package is installed.}

\item{n_threads}{Number of CPU worker threads used by CPU backends.}
}
\value{
A `faissR_nn` object with `indices` and `distances` matrices.
}
\description{
`nn_without_self()` is a convenience wrapper around `nn()` for the common
case where the reference and query data are the same matrix and the
self-neighbour should be removed. It returns exactly `k` non-self neighbours
per observation.
}
