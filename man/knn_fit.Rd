% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knn_model.R
\name{knn_fit}
\alias{knn_fit}
\alias{faiss.fit}
\alias{cuvs.fit}
\title{Fit a k-nearest-neighbour classifier or regressor}
\usage{
knn_fit(
  X_train,
  y_train,
  backend = "auto",
  metric = c("euclidean", "cosine", "correlation"),
  task = c("auto", "classification", "regression"),
  k = 15L,
  n_threads = NULL
)

faiss.fit(X_train, y_train, ...)

cuvs.fit(X_train, y_train, ...)
}
\arguments{
\item{X_train}{Numeric training matrix with observations in rows.}

\item{y_train}{Training labels or numeric response.}

\item{backend}{Neighbour backend passed to [nn()]. Use `"faiss"` for FAISS
CPU search or `"cuda"`/`"cuda_cuvs"` for CUDA/cuVS when available.}

\item{metric}{Distance metric passed to [nn()].}

\item{task}{`"auto"`, `"classification"`, or `"regression"`. `"auto"` treats
numeric responses as regression and other response types as classification.}

\item{k}{Default number of neighbours used by [predict()] and
[predict()] with \code{type = "prob"} when they are called without `k`.}

\item{n_threads}{CPU threads passed to [nn()] for CPU backends.}

\item{...}{Additional arguments passed to [knn_fit()] by the convenience
wrappers [faiss.fit()] and [cuvs.fit()].}
}
\value{
A fitted `faissR_knn_model` object.
}
\description{
`knn_fit()` stores a reference matrix and response vector, then uses [nn()]
during prediction. This keeps the model API small while still using the
package's FAISS, cuVS/CUDA, HNSW, grid, VP-tree, or exact CPU neighbour
backends. Classification supports majority or inverse-distance weighted
votes. Regression supports ordinary or inverse-distance weighted neighbour
averages.
}
\examples{
x <- scale(as.matrix(iris[, 1:4]))
clf <- knn_fit(x, iris$Species, backend = "cpu", k = 5)
head(predict(clf, x, k = 5))
head(predict(clf, x, k = 5, type = "prob"))

reg <- knn_fit(x, iris$Sepal.Length, backend = "cpu", task = "regression")
head(predict(reg, x, k = 5, vote = "weighted"))
}
